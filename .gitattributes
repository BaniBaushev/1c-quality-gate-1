# Фикстуры тестов не нормализуются: часть проверок различает переводы строк и BOM,
# и автоматическая нормализация превратила бы их в проверку не того, что задумано.
tests/fixtures/** -text

# Остальное — с нормализацией к LF в репозитории.
* text=auto eol=lf

# Фикстура часового: байты значимы (BOM), нормализация EOL недопустима
assets/analyzer/sentinel-fixture/** -text
